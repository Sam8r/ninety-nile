# Database (PostgreSQL)
DATABASE_URL=postgresql://nn:nn@localhost:5432/ninetynile

# Auth.js
AUTH_SECRET=replace-with-openssl-rand-base64-32

# Seeded admin user
ADMIN_EMAIL=admin@ninetynile.com
ADMIN_PASSWORD=ChangeMeNow123

# Media storage (local volume)
UPLOAD_DIR=./uploads

# SMTP (optional — enables contact-form email)
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASSWORD=
# SMTP_FROM="NinetyNile <hello@ninetynile.com>"

# Public site URL
NEXT_PUBLIC_SITE_URL=http://localhost:3000
